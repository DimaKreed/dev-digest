---
name: dev-digest-conventions
description: "House coding conventions for DimaKreed/dev-digest, extracted from the repository’s own code by DevDigest and verified against real file:line occurrences. Use when writing or reviewing TypeScript/React in this repository — co-located styles.ts objects, type-only imports from @devdigest/shared, and index.ts barrel re-exports. Trigger terms: convention, house rule, house style, code review, styles.ts, satisfies CSSProperties, import type, index.ts barrel, re-export."
---
# dev-digest-conventions

House conventions for `dev-digest`. Every rule below was verified against real code across 6 files before it was included.
Flag changes that violate any rule below and cite the offending `file:line`.

## co-locate-component-styles-in-a-styles-ts-file-typing-each-p

Co-locate component styles in a `styles.ts` file, typing each property with `satisfies CSSProperties` and the whole object with `as const`.

Seen in 2 files, e.g. `client/src/app/agents/[id]/_components/AgentEditor/_components/ConfigTab/styles.ts:8-18`:

```ts
  enabledLabel: {
    marginLeft: "auto",
    display: "flex",
    alignItems: "center",
    gap: 10,
    fontSize: 13,
    color: "var(--text-secondary)",
  } satisfies CSSProperties,
  actions: { display: "flex", gap: 10, marginTop: 10 } satisfies CSSProperties,
  savedNote: { alignSelf: "center", fontSize: 13, color: "var(--ok)" } satisfies CSSProperties,
} as const;
```

## import-shared-types-using-import-type-from-devdigest-shared

Import shared types using `import type` from `@devdigest/shared`.

Seen in 2 files, e.g. `client/src/app/agents/[id]/_components/AgentEditor/AgentEditor.tsx:9`:

```tsx
import type { Agent } from "@devdigest/shared";
```

## re-export-the-component-from-an-index-ts-file-using-both-nam

Re-export the component from an `index.ts` file using both named and default export.

Seen in 2 files, e.g. `client/src/app/agents/[id]/_components/AgentEditor/_components/ConfigTab/index.ts:1`:

```ts
export { ConfigTab, ConfigTab as default } from "./ConfigTab";
```

## Provenance

Generated by the DevDigest conventions extractor from `DimaKreed/dev-digest`, exported
`2026-08-04`. Three accepted rules, verified across 6 real files. Rejected and untriaged
candidates cannot reach this file by construction — `buildSkillDraft` filters to `accepted`
before rendering.

Do not hand-edit this file. Re-run the extractor and re-export the plugin bundle
(`GET /repos/:id/conventions/plugin`) instead.
