import type { CSSProperties } from "react";

export const s = {
  page: { padding: "24px 28px 48px", maxWidth: 1180 } satisfies CSSProperties,
  title: { fontSize: 24, fontWeight: 700, letterSpacing: "-0.02em" } satisfies CSSProperties,
  subtitle: {
    fontSize: 13,
    color: "var(--text-muted)",
    marginTop: 4,
  } satisfies CSSProperties,
  head: { display: "flex", alignItems: "flex-start", gap: 16 } satisfies CSSProperties,
  headActions: { marginLeft: "auto", display: "flex", gap: 8 } satisfies CSSProperties,
  sectionLabel: {
    display: "flex",
    alignItems: "center",
    gap: 8,
    fontSize: 11,
    fontWeight: 600,
    letterSpacing: "0.06em",
    color: "var(--text-muted)",
    margin: "22px 0 10px",
  } satisfies CSSProperties,
  agentRow: {
    display: "flex",
    alignItems: "center",
    gap: 18,
    padding: "14px 18px",
    border: "1px solid var(--border)",
    borderRadius: 10,
    background: "var(--bg-elevated)",
    marginBottom: 10,
    cursor: "pointer",
    textDecoration: "none",
    color: "inherit",
  } satisfies CSSProperties,
  agentIcon: {
    width: 34,
    height: 34,
    display: "grid",
    placeItems: "center",
    borderRadius: 8,
    background: "var(--bg-hover)",
    color: "var(--accent)",
    flexShrink: 0,
  } satisfies CSSProperties,
  agentMain: { minWidth: 0, flex: 1 } satisfies CSSProperties,
  agentName: {
    display: "flex",
    alignItems: "center",
    gap: 8,
    fontSize: 15,
    fontWeight: 700,
  } satisfies CSSProperties,
  agentMeta: {
    fontSize: 12,
    color: "var(--text-muted)",
    marginTop: 3,
  } satisfies CSSProperties,
  metricCell: { textAlign: "right", minWidth: 74 } satisfies CSSProperties,
  metricLabel: {
    fontSize: 10,
    fontWeight: 600,
    letterSpacing: "0.06em",
    color: "var(--text-muted)",
  } satisfies CSSProperties,
  metricValue: (color: string): CSSProperties => ({
    fontSize: 19,
    fontWeight: 700,
    color,
  }),
  table: {
    border: "1px solid var(--border)",
    borderRadius: 10,
    overflow: "hidden",
    background: "var(--bg-elevated)",
  } satisfies CSSProperties,
  tr: (clickable: boolean): CSSProperties => ({
    display: "flex",
    alignItems: "center",
    gap: 12,
    padding: "12px 16px",
    borderTop: "1px solid var(--border)",
    fontSize: 13,
    cursor: clickable ? "pointer" : "default",
    textDecoration: "none",
    color: "inherit",
  }),
  th: {
    display: "flex",
    alignItems: "center",
    gap: 12,
    padding: "10px 16px",
    fontSize: 11,
    fontWeight: 600,
    letterSpacing: "0.05em",
    color: "var(--text-muted)",
    background: "var(--bg-surface)",
  } satisfies CSSProperties,
  colAgent: { flex: 1, minWidth: 0 } satisfies CSSProperties,
  colWhen: { width: 160, flexShrink: 0 } satisfies CSSProperties,
  colVersion: { width: 66, flexShrink: 0 } satisfies CSSProperties,
  colMetric: { width: 96, flexShrink: 0, textAlign: "right" } satisfies CSSProperties,
  colPass: { width: 76, flexShrink: 0, textAlign: "right" } satisfies CSSProperties,
  bar: (v: number, color: string): CSSProperties => ({
    display: "inline-block",
    height: 5,
    width: `${Math.round(v * 56)}px`,
    background: color,
    borderRadius: 3,
    marginRight: 8,
    verticalAlign: "middle",
  }),
  barTrack: {
    display: "inline-block",
    width: 56,
    height: 5,
    background: "var(--border)",
    borderRadius: 3,
    marginRight: 8,
    verticalAlign: "middle",
  } satisfies CSSProperties,
  muted: { color: "var(--text-muted)" } satisfies CSSProperties,
} as const;
