import type { CSSProperties } from "react";

export const s = {
  page: { padding: "24px 28px 48px", maxWidth: 1180 } satisfies CSSProperties,
  back: {
    display: "inline-flex",
    alignItems: "center",
    gap: 6,
    fontSize: 13,
    color: "var(--text-muted)",
    textDecoration: "none",
    marginBottom: 14,
  } satisfies CSSProperties,
  head: { display: "flex", alignItems: "flex-start", gap: 14 } satisfies CSSProperties,
  title: {
    display: "flex",
    alignItems: "center",
    gap: 10,
    fontSize: 24,
    fontWeight: 700,
    letterSpacing: "-0.02em",
  } satisfies CSSProperties,
  subtitle: { fontSize: 13, color: "var(--text-muted)", marginTop: 4 } satisfies CSSProperties,
  headActions: { marginLeft: "auto", display: "flex", gap: 8, alignItems: "center" },
  alert: (regression: boolean): CSSProperties => ({
    display: "flex",
    gap: 10,
    alignItems: "flex-start",
    padding: "12px 16px",
    borderRadius: 9,
    fontSize: 13,
    lineHeight: 1.5,
    margin: "18px 0",
    border: `1px solid ${regression ? "var(--warn)" : "var(--border)"}`,
    background: regression ? "var(--warn-bg, rgba(180,120,20,0.10))" : "var(--bg-elevated)",
  }),
  tiles: { display: "flex", gap: 12, flexWrap: "wrap", marginBottom: 18 } satisfies CSSProperties,
  chartCard: {
    border: "1px solid var(--border)",
    borderRadius: 10,
    background: "var(--bg-elevated)",
    padding: 18,
    marginBottom: 22,
  } satisfies CSSProperties,
  chartHead: {
    display: "flex",
    alignItems: "center",
    gap: 8,
    fontSize: 11,
    fontWeight: 600,
    letterSpacing: "0.05em",
    color: "var(--text-muted)",
    marginBottom: 10,
  } satisfies CSSProperties,
  legend: { marginLeft: "auto", display: "flex", gap: 14, fontSize: 12 } satisfies CSSProperties,
  legendDot: (color: string): CSSProperties => ({
    display: "inline-block",
    width: 12,
    height: 2,
    background: color,
    marginRight: 6,
    verticalAlign: "middle",
  }),
  sectionLabel: {
    display: "flex",
    alignItems: "center",
    gap: 8,
    fontSize: 11,
    fontWeight: 600,
    letterSpacing: "0.06em",
    color: "var(--text-muted)",
    marginBottom: 10,
  } satisfies CSSProperties,
  table: {
    border: "1px solid var(--border)",
    borderRadius: 10,
    overflow: "hidden",
    background: "var(--bg-elevated)",
  } satisfies CSSProperties,
  th: {
    display: "flex",
    alignItems: "center",
    gap: 12,
    padding: "10px 16px",
    fontSize: 11,
    fontWeight: 600,
    letterSpacing: "0.05em",
    color: "var(--text-muted)",
    background: "var(--bg-surface)",
  } satisfies CSSProperties,
  tr: (selected: boolean): CSSProperties => ({
    display: "flex",
    alignItems: "center",
    gap: 12,
    padding: "12px 16px",
    borderTop: "1px solid var(--border)",
    fontSize: 13,
    background: selected ? "var(--bg-hover)" : "transparent",
  }),
  colCheck: { width: 26, flexShrink: 0 } satisfies CSSProperties,
  colWhen: { width: 150, flexShrink: 0 } satisfies CSSProperties,
  colVersion: { width: 70, flexShrink: 0 } satisfies CSSProperties,
  colMetric: { flex: 1, minWidth: 90, textAlign: "right" } satisfies CSSProperties,
  colPass: { width: 74, flexShrink: 0, textAlign: "right" } satisfies CSSProperties,
  colCost: { width: 70, flexShrink: 0, textAlign: "right" } satisfies CSSProperties,
  muted: { color: "var(--text-muted)" } satisfies CSSProperties,
  empty: { fontSize: 13, color: "var(--text-muted)", lineHeight: 1.6 } satisfies CSSProperties,

  // --- compare modal --------------------------------------------------------
  compareBody: { padding: "18px 24px" } satisfies CSSProperties,
  deltaGrid: {
    display: "grid",
    gridTemplateColumns: "repeat(auto-fit, minmax(160px, 1fr))",
    gap: 12,
  } satisfies CSSProperties,
  deltaCard: {
    border: "1px solid var(--border)",
    borderRadius: 9,
    padding: 14,
    background: "var(--bg-surface)",
  } satisfies CSSProperties,
  deltaLabel: {
    fontSize: 10,
    fontWeight: 600,
    letterSpacing: "0.06em",
    color: "var(--text-muted)",
  } satisfies CSSProperties,
  deltaRow: {
    display: "flex",
    alignItems: "baseline",
    gap: 8,
    marginTop: 8,
  } satisfies CSSProperties,
  deltaOld: { fontSize: 15, color: "var(--text-muted)" } satisfies CSSProperties,
  deltaNew: (color: string): CSSProperties => ({ fontSize: 21, fontWeight: 700, color }),
  deltaBadge: (v: number): CSSProperties => ({
    fontSize: 12,
    fontWeight: 600,
    color: v === 0 ? "var(--text-muted)" : v > 0 ? "var(--ok)" : "var(--crit)",
  }),
  diffBox: {
    marginTop: 8,
    border: "1px solid var(--border)",
    borderRadius: 9,
    background: "var(--bg-surface)",
    padding: "12px 0",
    maxHeight: 320,
    overflow: "auto",
  } satisfies CSSProperties,
  diffLine: (op: "same" | "added" | "removed"): CSSProperties => ({
    display: "block",
    padding: "1px 16px",
    fontSize: 12.5,
    lineHeight: 1.6,
    whiteSpace: "pre-wrap",
    wordBreak: "break-word",
    color:
      op === "same" ? "var(--text-secondary)" : op === "added" ? "var(--ok)" : "var(--crit)",
    background:
      op === "same"
        ? "transparent"
        : op === "added"
          ? "var(--ok-bg, rgba(30,140,80,0.12))"
          : "var(--crit-bg, rgba(180,50,50,0.12))",
  }),
  diffKey: {
    display: "flex",
    gap: 16,
    fontSize: 12,
    color: "var(--text-muted)",
    marginTop: 10,
  } satisfies CSSProperties,
  swatch: (color: string): CSSProperties => ({
    display: "inline-block",
    width: 9,
    height: 9,
    borderRadius: 2,
    background: color,
    marginRight: 6,
  }),
} as const;
